### Endo Archive (.agar)

This is an experiment in running [Endo Compartment-Mapper](https://github.com/endojs/endo/tree/master/packages/compartment-mapper) app archives in KumavOS.

Since `.agar` files are just `.zip` files, try renaming `calc.agar` to `calc.zip` and exploring the files.
